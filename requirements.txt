# Lina's Laser Bot - Python Dependencies
# =======================================

# Core Framework
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.1
python-multipart==0.0.31

# OpenAI & AI
openai>=1.57.0,<2.0.0

# HTTP Client (firebase-admin 7.3 requires httpx 0.28.1)
httpx==0.28.1

# Environment Variables
python-dotenv==1.2.2

# Firebase & Database
firebase-admin==7.3.0
google-cloud-firestore==2.23.0
# Pin grpc/protobuf explicitly to avoid long pip backtracking on Python 3.13 during deploy
grpcio==1.80.0
grpcio-status==1.80.0
protobuf>=6.31.1,<7.0.0
proto-plus>=1.25.0,<2.0.0

# Audio Processing
pydub==0.25.1
audioop-lts>=0.2.0; python_version >= "3.13"  # audioop removed from stdlib in 3.13+

# Image Processing (>=11.0 for Python 3.13 compatibility)
Pillow>=11.0.0

# Async Support
aiofiles==23.2.1

# JSON & Data Processing
jsonlines==4.0.0

# Date & Time
python-dateutil==2.8.2

# Task Scheduling
apscheduler==3.10.4

# Optional: For production deployment
gunicorn==23.0.0

# Optional: For monitoring
prometheus-client==0.19.0

# Password Hashing
bcrypt>=4.0.0

# Stripe Checkout (optional — wallet recharge when STRIPE_SECRET_KEY is set)
stripe>=11.0.0,<13.0.0

# Authenticated encryption for tenant Meta access tokens at rest
cryptography==50.0.0
PyJWT==2.13.0

# Language Detection for English/French (used in language_resolver.py).
# This maintained wheel preserves the `langdetect` import API and avoids an
# unreviewed source build during the immutable HA release transaction.
langdetect-py==1.1.1

# Durable job queues (production workers). Connection via REDIS_URL / LINAS_REDIS_URL.
redis>=5.0.0,<6.0.0

# WhatsApp Cloud coexistence PostgreSQL SoT
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.12
