# Dev / CI quality tools. `requirements-dev.lock` resolves this file together
# with requirements.txt so tests and linters share the exact production pins.
pip==26.2.1
pytest>=9.0.3,<10
pytest-asyncio>=0.23.0,<1.0
anyio>=4.0.0,<5
ruff>=0.6.0
mypy>=1.8.0
types-requests
bandit>=1.7.0
pip-audit>=2.6.0
fakeredis>=2.23.0,<3.0.0
