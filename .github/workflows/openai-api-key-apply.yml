name: BLOCKED - OpenAI API Key Apply Requires Two-Node HA

on:
  workflow_dispatch:
    inputs:
      CONFIRM:
        description: "Type APPLY_OPENAI_API_KEY to acknowledge the blocked operation"
        required: true
        type: string

concurrency:
  group: meta-social-cutover
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  blocked:
    if: github.event.inputs.CONFIRM == 'APPLY_OPENAI_API_KEY'
    runs-on: ubuntu-latest
    environment: meta-social-cutover
    steps:
      - name: Refuse node01-only environment mutation
        run: |
          echo "BLOCKED: OPENAI_API_KEY needs an exact two-node env backup, sync, restart, worker proof, and rollback transaction." >&2
          exit 1
