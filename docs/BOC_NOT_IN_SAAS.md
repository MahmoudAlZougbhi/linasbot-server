# BOC is not in SaaS.

The Linas Laser clinic booking/CRM stack (`services/booking/**`, `api_integrations*`,
`boc-lb.com`) is deleted from this product. Customer Requests stay on
`services/requests`. Do not re-enable BOC HTTP or `BOC_BOOKING_ENABLED`.
