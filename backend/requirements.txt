# STALE — DO NOT USE as a dependency source of truth.
#
# Canonical Python dependencies: ../requirements.txt (repository root).
# backend/Dockerfile* COPY the root requirements.txt (build context = repo root).
# This duplicate historically pinned older fastapi/openai and drifted from prod.
#
# Intentionally empty of packages so `pip install -r backend/requirements.txt`
# installs nothing. Always use:
#   pip install -r requirements.txt
